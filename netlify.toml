# Netlify Configuration for Mono-repo root build
[build]
  base = "healthtrack-frontend"
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
